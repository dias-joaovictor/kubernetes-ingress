apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-nginx
  labels:
    name: ingress-nginx
spec:
  rules:
  - host: 168.61.38.244
    http:
      paths:
      - pathType: Prefix
        path: "/nginx_status"
        backend:
          service:
            name: ingress-nginx
            port: 
              number: 18080
